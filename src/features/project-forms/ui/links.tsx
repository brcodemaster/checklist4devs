'use client'

import { Input } from '@/shared/ui'

import { useProjectForms } from '../model'

export const Links: React.FC = () => {
	const { append, fields, remove, update } = useProjectForms()

	return (
		
	)
}
